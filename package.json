{
  "name": "nodejs",
  "version": "1.0.0",
  "main": "index.js",
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "start": "node index.js",
    "dev": "nodemon index.js",
    "build": "cd frontend && npm install --production=false && npm run build",
    "prod_build": "npm install && cd frontend && npm install --production=false && npm run build"
  },
  "type": "module",
  "dependencies": {
    "@types/node": "^20.0.0",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.21.1",
    "mongoose": "^8.8.1"
  },
  "devDependencies": {
    "nodemon": "^3.1.7"
  }
}